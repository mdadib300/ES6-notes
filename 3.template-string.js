// template strings - ` write here `
// to use variables in it - `My age is ${variable_name}.`

const name = 'Adib';
let age = 21;

const info = `Hello, this is ${name}. I'm ${age} years old and I'm using template string.
See? It can use new line without using any escape sequence!`;

console.log(info);